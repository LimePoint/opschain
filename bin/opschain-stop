#!/usr/bin/env bash
set -eo pipefail -o nounset

CDPATH='' cd "$(dirname "${BASH_SOURCE[0]}")"

exec ./opschain-deploy --set stopped=true --set trow.replicaCount=0 --set kong.replicaCount=0 --set kong.proxy.enabled=false "$@"
